<html>
    <body>
        <p>You received a ticket for Drupal Camp Transylvania!</p>
        {% if codes|length == 1 %}
            <p>Your ticket coupon is:</p>
        {% else %}
            <p>Your ticket coupon are:</p>
        {% endif %}
        {% for code in codes %}
            <p><b>{{ code }}</b></p>
        {% endfor %}
        <p>Click here to redeem the coupon(s): {{ redeem_link }}</p>
    </body>
</html>
